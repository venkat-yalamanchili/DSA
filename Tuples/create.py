newtuple = (1,2,3,4)

print(newtuple , type(newtuple))

newtuple1 = ('a',)  # THis is how u declare single element in tuple newtuple1 = ('a') this is wrong as consider as string

newtuple2 = tuple()  # empty tuple